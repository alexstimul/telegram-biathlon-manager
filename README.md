# Biathlon Manager Bot (MVP)

Telegram bot on Node.js + TypeScript (grammY).  
Current state: responds to `/start` with a welcome message.

## Requirements
- Node.js 18+ (recommended 20+)
- npm

## Setup
1) Install deps:
```bash
npm install
